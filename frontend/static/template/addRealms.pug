extends layout 

block content 
    .container
        h1.text-center 新開地點
        h2 新增位置（學校）
        input.form-control(type="text", name="")
        hr
        h2 新增場館
        input.form-control(type="text", name="")
        